RECIPES:  SINGLETITLE:

1. Warning: Can't perform a React state update on an unmounted component. This is a no-op, but it indicates a memory leak in your application. To fix, cancel all subscriptions and asynchronous tasks in a useEffect cleanup function.

PROFILE:

2. No routes matched location "/profile"

3. Warning: A component is changing an uncontrolled input to be controlled. This is likely caused by the value changing from undefined to a defined value, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the componen

4. MAPCHART: Error: <g> attribute transform: Expected number, "translate(NaN, NaN)".

5. MY FOOD: Sub-Menu stays open

6. Page-refresh occuring even with routing??

7. Mixed Content: The page at 'https://61e0087c7bcada13e105f218--vrwgh-finalprojectwd020.netlify.app/profile' was loaded over HTTPS, but requested an insecure stylesheet 'http://fonts.cdnfonts.com/css/amble'. This request has been blocked; the content must be served over HTTPS.
