import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Share my food',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
